<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lambda.voicechatroom">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".calls.InCallActivity"></activity>
        <activity
            android:name=".group_details.GroupDetailsActivity"
            android:label="@string/title_activity_group_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".groups.ViewGroupsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".edit_profile.EditProfileActivity" />
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.FirebaseOauthActivity" />
    </application>

</manifest>